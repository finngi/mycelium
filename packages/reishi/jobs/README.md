# jobs/

RayJob template + Dockerfile land here: one `rayjob.yaml` templated by
`mcm experiment submit`, worker groups per accelerator (`l4`, `h100`, `v5e`),
image pushed to the `ml-models` Artifact Registry repo.
