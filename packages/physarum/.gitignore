.venv/
__pycache__/
*.egg-info/
.pytest_cache/
uv.lock
.DS_Store

# experiment-local data pulls (e.g. HF dataset extracts) -- not source, and
# large; regenerate via each experiment's fetch script rather than commit
experiments/*/data/
